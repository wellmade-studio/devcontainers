name: build

on:
  push:
    branches: [main]
    paths:
      - "images/**"
      - "catalog.toml"
      - ".github/workflows/build.yml"
  pull_request:
    paths:
      - "images/**"
      - "catalog.toml"
      - ".github/workflows/build.yml"
  schedule:
    # Weekly rebuild so upstream apt/npm/pip patches flow through.
    - cron: "0 5 * * 1"
  workflow_dispatch:

# Workflow-level permissions are inherited, but several action runtimes
# treat per-job permissions differently for OIDC token scoping.
# Declared on each job for safety.
permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/wellmade-studio/devcontainers

jobs:
  build-core:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v6

      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push core
        uses: docker/build-push-action@v6
        with:
          context: images/core
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            ${{ env.IMAGE_PREFIX }}/core:v1
            ${{ env.IMAGE_PREFIX }}/core:latest
          # Cache only on push events (where we're authenticated to the
          # registry). PRs build without cache for safety. `ignore-error`
          # on cache-from lets first-run builds proceed when the cache
          # image doesn't exist yet (GHCR returns 403 for missing public
          # packages, not 404 — without this flag, buildx aborts).
          cache-from: |
            type=registry,ref=${{ env.IMAGE_PREFIX }}/core:buildcache,ignore-error=true
          cache-to: ${{ github.event_name != 'pull_request' && format('type=registry,ref={0}/core:buildcache,mode=max', env.IMAGE_PREFIX) || '' }}

  build-workbench:
    runs-on: ubuntu-latest
    needs: build-core
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v6

      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push workbench
        uses: docker/build-push-action@v6
        with:
          context: images/workbench
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          build-args: |
            CORE_TAG=v1
          tags: |
            ${{ env.IMAGE_PREFIX }}/workbench:v1
            ${{ env.IMAGE_PREFIX }}/workbench:latest
          cache-from: |
            type=registry,ref=${{ env.IMAGE_PREFIX }}/workbench:buildcache,ignore-error=true
          cache-to: ${{ github.event_name != 'pull_request' && format('type=registry,ref={0}/workbench:buildcache,mode=max', env.IMAGE_PREFIX) || '' }}
